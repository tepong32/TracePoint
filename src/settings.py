"""
Legacy default Django settings module.

This file is not in use for TracePoint.

Use the split settings package instead:
- src.settings.base
- src.settings.dev
- src.settings.prod

The project's entrypoints default to `src.settings.dev`, and production can
select `src.settings.prod` via `DJANGO_SETTINGS_MODULE` or `DJANGO_ENV`.
"""
